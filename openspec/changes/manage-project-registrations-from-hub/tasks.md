## 1. Dependency and Baseline

- [ ] 1.1 Complete and verify `harden-stable-project-navigation`, then record the stable project-route, child-lifecycle, compatibility, and release interfaces this change consumes; run `npm run openspec -- validate harden-stable-project-navigation --strict --no-interactive` and `npm test`, showing the predecessor artifacts and implementation are green before editing overlapping Hub routes.
- [ ] 1.2 Add failing characterization fixtures for trusted-Hub mutation rejection, version-1 registry reads, unavailable registrations, branch-without-worktree behavior, and consumer-code non-execution; run `npm test` and record that only the new intended assertions fail while unrelated tests remain green.
- [ ] 1.3 Exercise a direct, read-only Standard Additions folder-picker spike under the actual PM2 login GUI session with temporary output and no registry mutation; record foreground, cancel, timeout, protected-folder, spaces, Unicode, and embedded-newline evidence, and update the design before implementation if the Swift fallback becomes necessary.

## 2. Candidate Inspection and Native Picker

- [ ] 2.1 Add a bounded structural candidate inspector that requires the selected canonical folder to equal its Git top level, validates a contained regular `openspec/config.yaml`, classifies primary/linked/detached worktrees, and executes no consumer scripts, hooks, optional locks, filesystem monitors, or dirty-state scan; run `npm test` to show valid, nested, symlink, missing, unreadable, bare, submodule, non-OpenSpec, Unicode, and newline-path fixtures.
- [ ] 2.2 Add an injectable singleton folder-picker interface and a low-overhead macOS Standard Additions AppleScript adapter using direct process invocation, an unambiguous absolute POSIX path result, timeout, cancellation, shutdown cleanup, and typed platform/GUI/permission/process errors; run `npm test` with parser and fake-adapter coverage for spaces, Unicode, embedded newlines, cancel, busy, timeout, no-GUI, denial, malformed output, and orphan-free shutdown behavior.
- [ ] 2.3 Add a managed-Mac smoke script that uses temporary state and never writes a consumer repository; verify the real picker returns exactly the chosen canonical root and produces actionable failure output when the GUI boundary is unavailable.

## 3. Stable Registry Identity and Rebind

- [ ] 3.1 Implement a version-2 registry reader with stable opaque `id`, mutable canonical `root`, and numeric `revision`; preserve version-1 ids, labels, and roots in memory and write version 2 only during an authorized mutation; run `npm test` to show v1 compatibility, private permissions, atomic migration, malformed-version failure, and rollback-backup integrity.
- [ ] 3.2 Add normalized label validation, duplicate-canonical-root detection, explicit already-registered results, and atomic add/rename operations under the owner-aware lock; run `npm test` to show printable 1-120-character labels succeed while control, bidi, zero-width, duplicate, concurrent, and partial-write cases fail closed.
- [ ] 3.3 Add compare-and-swap rebind over registration id, expected revision, and preceding root; preserve the stable id, increment revision, retain or edit the label, and leave the old record intact on any failure; run `npm test` to show moved-root success, different-repository confirmation, stale-tab conflict, duplicate-root rejection, confirmation-time substitution, and concurrent rebind integrity.
- [ ] 3.4 Connect registry revision changes to the predecessor launcher lifecycle so committed rebind invalidates preceding children and pending launches and stable routes re-resolve before publication; run `npm run test:e2e` to show an old child cannot serve after rebind while the stable bookmark resolves only the confirmed new root.
- [x] 3.5 Add compare-and-swap removal by stable registration id and expected revision, returning the removed record without accepting or deleting paths; run `npm test` to show success, stale revision, unknown id, concurrent removal, and byte-for-byte consumer filesystem preservation.

## 4. Ephemeral Registration Intents and Security

- [ ] 4.1 Implement a two-minute in-memory registration-intent state machine for add and rebind with opaque ids, Hub-session binding, single use, polling states, cancellation, expiry, restart loss, and deterministic cleanup; run `npm test` to show every permitted transition plus double-confirm, expiry, cancellation race, restart, and concurrent-picker behavior.
- [x] 4.2 Add a same-origin Hub bootstrap value and narrow intent create/read/confirm/cancel routes restricted to trusted-proxy mode with exact Host, Origin, request-target, fetch-metadata, application-header, CSRF, JSON content-type, body-size, method, and closed-schema enforcement; run `npm run test:security` to show hostile or malformed requests are rejected before the picker or filesystem inspector runs.
- [x] 4.3 Reinspect the server-held root and execute bounded confirmed-project compatibility verification only after explicit confirmation, then commit through the registry compare-and-swap without exposing raw command output; run `npm test` to show malicious consumer commands cannot run during preview, unsupported compatibility fails without mutation, TOCTOU changes fail, and a valid confirmation commits once.
- [x] 4.4 Add the trusted bodyless `DELETE /api/projects/<project-id>` route with exact authority, CSRF, id, quoted `If-Match` revision, and no-query validation; commit registry removal before child invalidation and run `npm run test:security` for hostile, stale, path-bearing, and malformed requests.

## 5. Worktree Discovery Semantics

- [ ] 5.1 Parse NUL-delimited Git worktree evidence and classify same-repository readable OpenSpec, missing, locked, prunable, detached, non-OpenSpec, duplicate-branch, and foreign-common-directory records without returning raw paths; run `npm test` to show exact classifications for spaces, Unicode, embedded newlines, stale records, and path substitution.
- [ ] 5.2 Preserve branch visibility independently from openable worktrees and remove vanished worktree records without tombstones or borrowed OpenSpec state; run `npm run test:e2e` to show openable, deleted, pruned, non-OpenSpec, branch-only, and valid-empty-OpenSpec scenarios without Git mutation.

## 6. Approved Hub UX

- [x] 6.1 Add the approved `Add project` header action, picker-progress state, labelled candidate dialog, canonical path and validation summary, editable display-name field, confirm/cancel controls, and focus restoration using translation-catalogue copy; run `npm test` and the keyboard walkthrough to show accessible names, focus containment, polite announcements, cancellation, validation, and successful add.
- [x] 6.2 Replace the unavailable-card dead end with the approved `Find new folder` flow showing old/new locations and an explicit stable-slot redirect confirmation, while retaining normal open behavior for available cards; run `npm run test:e2e` to show rebind success, different-repository warning, stale conflict, failed rebind preservation, and stable bookmark behavior.
- [x] 6.3 Add responsive styles and typed localized error states for picker unavailable, busy, cancelled, timed out, permission denied, invalid OpenSpec, already registered, compatibility failure, expired intent, and conflict; run the approved browser walkthrough at desktop and narrow widths in both color schemes and record project owner's final visual confirmation before marking this task complete.
- [x] 6.4 Add the approved localized `Remove from Hub` card action and confirmation explaining that project, Git, worktree, and OpenSpec files remain untouched; verify keyboard focus, cancellation, success, conflict, available and unavailable cards, and narrow layouts.

## 7. Verification, Documentation, and Controlled Release

- [ ] 7.1 Update README and release documentation for site-based registration, Finder selection, immediate renaming, rebind behavior, worktree availability, macOS GUI requirements, security boundaries, and CLI recovery fallback; verify every command and path with `npm run verify`.
- [x] 7.2 Run `npm run openspec -- validate manage-project-registrations-from-hub --strict --no-interactive`, `npm run typecheck`, `npm test`, `npm run test:e2e`, `npm run test:security`, `npm run verify:bundle`, and `npm audit`; record exact passing evidence and review the scoped diff without touching reference consumer or any consumer repository.
- [ ] 7.3 With separate explicit activation authorization, publish the deterministic release, back up the pre-migration registry, switch only the OpenSpec Workbench runtime, restart only its PM2 process, and verify `https://plans.internal` add/rebind plus hostile-request cases; record that unrelated processes, consumer files, Git state, worktrees, and OpenSpec artifacts are unchanged.
- [ ] 7.4 Verify rollback as one operation by restoring the preceding runtime and compatible pre-migration registry backup, then confirm the prior Hub still lists and opens the original registrations; leave push, merge, tag, deployment beyond the managed Mac, and backup deletion unperformed without separate authorization.
